//Напишете функцията повдигане 
//на степен, която повдига х на степен у

#include <stdio.h>
#include <math.h>

#define raiseNum(x, y) res = pow(x, y);
int main() {
int res;
raiseNum(5, 3)
printf("%d", res);
}
