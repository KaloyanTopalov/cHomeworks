#include <stdio.h>
#include <string.h>
int main ()
{
char s[]="CodeAcademy\0";
int lenWord = strlen(s);
int n;
printf("letter: ",n);
scanf("%d", &n);
for(int i = 0; i <= n; i++ )
{
}
printf("Is: %c", n[s-1]);
printf("\nNumber of letters: %d", lenWord);
}
